"""clinical-rl: OpenEnv environment for clinical diagnostic reasoning.

The env implementation lives in `clinical_rl.env`. This top-level package
stays light so import-time side effects (e.g. yaml I/O) are opt-in.
"""
