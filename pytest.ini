[pytest]
testpaths = tests
addopts = -q
# Make `clinical_rl` (vendored inside house_md_env/) importable from the test
# suite as a bare top-level module, mirroring the in-Docker PYTHONPATH.
pythonpath = . house_md_env
norecursedirs = house_md_env results docs scripts notebooks
